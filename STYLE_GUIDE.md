# 写作与排版规范

这份规范用于保持两套教程的可读性和可维护性。它不是机械模板，章节复杂度不同时可以调整结构，但读者应始终能够判断本章解决什么问题、怎样应用以及如何验证。

## 读者与语言

- 默认读者是产品经理、项目负责人和需要参与 Agent 交付的跨职能成员。
- 不要求读者具备模型训练或 Agent 编程经验。
- 首次出现的英文术语说明它解决的问题，后文不重复解释。
- 保留必要的 `Agent`、`Prompt`、`Context`、`Trace` 等行业术语，不为了中文化制造生僻译名。
- 中英文之间保留一个半角空格，代码、链接和文件路径除外。

## 内容原则

每个重要机制至少回答四个问题：

1. 它解决什么产品问题；
2. 产品经理需要作出什么判断；
3. 它与其他机制的责任边界在哪里；
4. 团队怎样验证它真的有效。

内容优先使用具体任务、状态、动作和证据。避免只写“提升体验”“增强能力”“保障安全”这类无法验收的结论。

## 两套教程的分工

- `book1` 解释产品经理应该提出什么要求，沿产品生命周期组织。
- `book2` 解释这些要求怎样落成信息、状态、协议、运行时和评估机制，按工程责任组织。
- `book2` 需要回顾入门概念时，用简短链接承接，不重复整段方法。

## 标题

- 每个文件只有一个一级标题。
- 二级标题表达本章的主要判断或任务阶段。
- 三级标题用于拆分同一个二级主题，不直接从一级标题跳到三级标题。
- 标题不手动编号，章节顺序由文件名和目录控制。
- 避免连续使用只有名词、没有判断关系的标题。

## 段落与列表

- 一个段落集中说明一个判断，过长时按逻辑转折拆分。
- 流程、优先级和操作步骤使用有序列表。
- 并列规则、检查项和候选项使用无序列表。
- 表格用于对照，不在紧邻正文中逐行复述表格。
- `本章小结` 只保留本章结论，不引入新概念。
- `思考题` 要求读者应用本章方法，不只是回忆名词。

## 图示

- 图示必须有能独立说明内容的替代文本。
- 图示后的图注解释读者应该观察什么，不复述图中全部节点。
- Mermaid 源文件放在 `diagrams/source/`，生成 SVG 放在 `diagrams/svg/`。
- 正文引用 SVG；需要修改图示时先修改 Mermaid 源文件，再运行同步脚本。
- 章节中的概念插画放在对应 `book1/image/` 或 `book2/image/`。

## 代码块和示例

- 代码块只展示理解产品机制所需的最小结构。
- 示例必须说明它是协议、伪代码、数据结构还是可运行代码。
- 不省略会改变安全、权限、状态或副作用语义的关键字段。
- 特定框架示例同时说明框架无关的产品判断。

## 引用和链接

- 外部事实、数据和框架行为尽量链接到一手资料。
- 链接文本说明资料内容，不使用孤立的“点击这里”。
- 延伸阅读只保留对本章有直接帮助的资料，并用一句话说明阅读价值。
- 来源较多且正文使用编号引用时，统一放入 `参考资料`，不再在 `延伸阅读` 中重复列出。

## 章节收尾

普通正文统一按以下顺序收尾：

1. `本章小结`
2. `思考题`
3. `延伸阅读`或`参考资料`，没有必要资料时可以省略
4. 上一章、目录、下一章导航

引言和后记不强制使用这套结构。
